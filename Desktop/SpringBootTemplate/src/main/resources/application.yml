server:
  port: 9000  # 9000번 포트로 실행

spring:
  application:
    name: demo

  datasource:
    platform: mysql
    url: jdbc:mysql://54.180.1.233:3306/UMC
    # url 형식은 jdbc:mysql://RDS의 EndPoint/내부 DB 스키마명 입니다.
    username: kym2675 # 만약 따로 설정하지 않으셨다면 기본값은 admin 입니다
    password: dbals3319 # 비밀번호를 입력해주세요!
    driver-class-name: com.mysql.cj.jdbc.Driver
